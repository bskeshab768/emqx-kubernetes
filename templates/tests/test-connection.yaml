apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "empx.fullname" . }}-test-connection"
  labels:
    {{- include "empx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "empx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
